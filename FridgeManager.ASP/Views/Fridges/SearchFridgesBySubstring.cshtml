@model IEnumerable<FridgeDto>

@{
    ViewData["Title"] = "Fridges";
}

<div>
    <h3>Getting Fridges By Letters Which Fridge Name Contains</h3>

    <form class="getting-form" asp-action="SearchFridgesBySubstring" method="post">
        <label>Substring:</label>
        <input class="input-props" type="text" name="substring"/>
        <br />
        <div class="action-btn-container">
            <button class="action-btn" type="submit">Get</button>
            <a class="back-btn" href="/Home/Index">Back</a>
        </div>
    </form>  

    @if (@Model != null)
    {
        <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Owner Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                @foreach (var fridge in @Model)
                {
                    <tr>
                        <td>@fridge.Id</td>
                        <td>@fridge.FullModelName</td>
                        <td>@fridge.OwnerName</td>
                    </tr>
                }
            </tr>
        </tbody>
    </table>
    }
</div>